# Copy to `.env` and fill in values as integrations are added.
# Never commit real secrets.

# Logging (loguru) — TRACE | DEBUG | INFO | SUCCESS | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_FILE=vector-floorplan-ai.log
LOG_TO_CONSOLE=true
LOG_TO_FILE=true
